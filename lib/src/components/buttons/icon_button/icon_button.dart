import 'package:flutter/material.dart';

class AdaptiveIconButton extends IconButton {
  const AdaptiveIconButton({
    super.key,
    super.autofocus,
    super.onPressed,
    super.hoverColor,
    super.alignment,
    super.disabledColor,
    super.color,
    super.constraints,
    required super.icon,
  });
}
